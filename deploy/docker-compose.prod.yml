services:
  app:
    build:
      context: ..
      dockerfile: build/Dockerfile
    container_name: personal_disk_app_prod
    restart: unless-stopped
    environment:
      APP_ENV: production
      DB_DRIVER: ${DB_DRIVER:-sqlite}
      DB_PATH: ${DB_PATH:-data/personal_disk.db}
      DB_HOST: ${DB_HOST:-127.0.0.1}
      DB_PORT: ${DB_PORT:-3306}
      DB_USER: ${DB_USER:-root}
      DB_PASSWORD: ${DB_PASSWORD:-}
      DB_NAME: ${DB_NAME:-personal_disk}
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: 8080
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?ADMIN_PASSWORD is required}
      UPLOAD_DIRECTORY: storage
      UPLOAD_MAX_SIZE: ${UPLOAD_MAX_SIZE:-536870912}
    volumes:
      - sqlite-prod-data:/app/data
      - storage-prod-data:/app/storage
    ports:
      - "${APP_PORT:-8080}:8080"

volumes:
  sqlite-prod-data:
  storage-prod-data:
